<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Página personal o proyecto elegante">
    <title>Mi Proyecto en GitHub</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Roboto:wght@300&display=swap" rel="stylesheet">
    
    <!-- Estilos básicos -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            line-height: 1.6;
            background-color: #f7f7f7;
            color: #333;
        }

        section {
            max-width: 800px;
            margin: 0 auto; /* Centrar las secciones */
            padding: 60px 20px; /* Espacio superior e inferior */
            background-color: white;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.6s ease-out, transform 0.6s ease-out;
            margin-bottom: 40px; /* Espacio entre secciones */
        }

        h1, h2 {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
            color: #333;
            margin-bottom: 20px; /* Margen entre título y contenido */
        }

        p {
            font-size: 1.2rem;
            margin-bottom: 20px;
            font-weight: 300;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            font-size: 1rem;
            color: #0073e6;
            border: 2px solid #0073e6;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        a:hover {
            background-color: #0073e6;
            color: white;
        }

        footer {
            margin: 40px 0;
            font-size: 0.9rem;
            color: #999;
            text-align: center;
        }

        /* Clases de animación para scroll */
        .scroll-anim {
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.6s ease-out, transform 0.6s ease-out;
        }

        .scroll-anim.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- Secciones con contenido -->
    <section class="scroll-anim">
        <h1>Bienvenido a Mi Proyecto</h1>
        <p>Este es un espacio donde comparto mi trabajo, código y proyectos interesantes. Estoy siempre buscando nuevas maneras de mejorar y aprender.</p>
        <a href="https://github.com/tuusuario" target="_blank">Ver en GitHub</a>
    </section>

    <section class="scroll-anim">
        <h2>Acerca de Mí</h2>
        <p>Soy un desarrollador apasionado por la tecnología, con experiencia en proyectos de código abierto, soluciones de software y desarrollo web. Me encanta aprender constantemente nuevas tecnologías y compartir mi conocimiento con la comunidad.</p>
    </section>

    <section class="scroll-anim">
        <h2>Proyectos Destacados</h2>
        <p>Aquí puedes encontrar algunos de los proyectos más recientes en los que he trabajado, que incluyen aplicaciones web, herramientas de automatización y bibliotecas de código.</p>
        <a href="#proyectos" target="_blank">Ver más</a>
    </section>

    <footer>
        &copy; 2024 Tu Nombre | Hecho con ♥ y GitHub Pages
    </footer>

    <!-- JavaScript para la animación de scroll -->
    <script>
        // Función que agrega la clase 'visible' al hacer scroll
        function handleScroll() {
            const sections = document.querySelectorAll('.scroll-anim');
            sections.forEach(section => {
                const sectionPosition = section.getBoundingClientRect().top;
                const screenPosition = window.innerHeight / 1.3;
                
                if (sectionPosition < screenPosition) {
                    section.classList.add('visible');
                }
            });
        }

        // Detectar el evento de scroll
        window.addEventListener('scroll', handleScroll);
        window.addEventListener('load', handleScroll);
    </script>
</body>
</html>
